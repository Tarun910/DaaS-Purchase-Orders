# DaaS Purchase Order Receiving

Take-home implementation of one DaaS roadmap feature slice (Option 1 — Sprint 2 — Purchase Order Receiving).

See the full setup instructions after the API and web packages are ready. For now:

```bash
docker compose up -d
npm install
```
