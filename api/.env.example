# Database
DATABASE_URL="postgresql://daas:daas@localhost:5432/daas?schema=public"

# Server
PORT=4000
NODE_ENV=development

# Simple bearer tokens mapped to roles (development only)
# Authorization: Bearer <token>
AUTH_TOKEN_ADMIN=admin-token
AUTH_TOKEN_WAREHOUSE=warehouse-token
AUTH_TOKEN_VIEWER=viewer-token
